/**
 * 
 */
package emulator;

/**
 * The class to identify emulator's different modes based on Arduino Sketch
 * 
 * @author CSR
 *
 */
public enum EmulatorModes
{
	CAPTURE, ATTACK, SUBMIT, REPLACE, ANSWER, STANDBY, DISCONNECTED;
}
